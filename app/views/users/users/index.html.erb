ユーザー
<% @users.each do |user| %>
  <%= link_to user_path(user.id) do %>
    <%= attachment_image_tag user, :profile_image, :fill, 60, 60, fallback: "noimage.png" %>
    <%= user.name %>
    <%= user.introduction %>
    <%= user.job_category %>
  <% end %>
<% end %>